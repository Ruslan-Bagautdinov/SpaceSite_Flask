FROM python:3.11-slim-bullseye

WORKDIR /app

COPY . /app

RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 5000

# Command to initialize the database and then start the Flask app
CMD ["sh", "-c", "python init_db.py && flask run --host=0.0.0.0"]
